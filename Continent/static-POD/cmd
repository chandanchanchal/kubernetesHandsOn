#-You can verify this by checking the kubelet config:
ps -ef | grep kubelet | grep -i static-pod
#-check the kubelet config file (location varies by distribution):
cat /var/lib/kubelet/config.yaml | grep staticPodPath
#-Create a YAML file in the static pods directory. For this example, we'll create an Nginx web server
sudo nano /etc/kubernetes/manifests/nginx-static-pod.yaml
